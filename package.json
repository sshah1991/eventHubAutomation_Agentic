{
  "name": "eventhubautomation_agentic",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test --grep @smoke",
    "test:sanity": "playwright test --grep @sanity",
    "test:regression": "playwright test --grep @regression",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report test-results",
    "report:allure": "npx allure generate -o allure-report && npx allure open allure-report",
    "report:allure:serve": "npx allure serve allure-results",
    "test:allure": "playwright test && npx allure generate -o allure-report && npx allure open allure-report",
    "test:regression:allure": "playwright test --grep @regression && npx allure generate -o allure-report && npx allure open allure-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.9.1",
    "allure-playwright": "^3.9.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  }
}
